module.exports = {
  printWidth: 120, // Specify the line length that the printer will wrap on.
  tabWidth: 2, // Specify the number of spaces per indentation-level.
  useTabs: false, // Indent lines with tabs instead of spaces.
  semi: true, // Print semicolons at the ends of statements.
  singleQuote: false, // Use single quotes instead of double quotes.
  trailingComma: "none", // Print trailing commas wherever possible when multi-line.
  bracketSpacing: true, // Print spaces between brackets in object literals.
  jsxBracketSameLine: false, // Put the > of a multi-line JSX element at the end of the last line instead of being alone on the next line.
  arrowParens: "avoid", // Include parentheses around a sole arrow function parameter.
  // htmlWhitespaceSensitivity: "css", // Control whitespace sensitivity in HTML and related languages.
  quoteProps: "as-needed", // Determine when to quote properties in objects.
  requirePragma: false // Format only files with a special `@format` or `@prettier` marker.
};
